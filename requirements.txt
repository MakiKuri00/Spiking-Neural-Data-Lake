# The stdlib prototypes (spiking_storage_prototype, test_prototype,
# snn_classifier, snn_moe_classifier) need NOTHING — pure Python standard library.
#
# Only the real-data / snnTorch files need these. CPU build is fine:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   pip install snntorch numpy
# GPU (for eth_mnist_bindsnet.py --gpu). RTX 5070 = Blackwell (sm_120) -> CUDA 12.8+:
#   pip install --force-reinstall torch torchvision --index-url https://download.pytorch.org/whl/cu128
torch
torchvision
snntorch
numpy

# v0.9 only — BindsNET Diehl & Cook runner (eth_mnist_bindsnet.py).
# NOTE: BindsNET <=0.3 imports torch._six (removed in torch >=2.0); the runner
# ships a built-in shim so it works on modern torch — no manual patching needed.
bindsnet

# v0.13 GPU port only — Paradigm B on GeNN (paradigm_b_genn.py). Needs a C++ compiler
# + CUDA 12.8+ (RTX 5070 = Blackwell). The CPU reference (paradigm_b_matcher.py) needs
# nothing. On a GPU box:  pip install pygenn
pygenn

# v0.26 lakehouse PoC only — lakehouse/medallion.py (Medallion Bronze/Silver/Gold over
# Parquet; polars is the single-node Spark/Delta substitute). Best in its own venv.
polars
